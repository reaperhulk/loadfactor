// Injected by vite.config.ts at build time. Not available to the engine or
// harness — this is display-only chrome, and the engine stays free of any
// value that changes between builds.
declare const __BUILD_SHA__: string
declare const __BUILD_TIME__: string
